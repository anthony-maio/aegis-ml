# OpenRouter API Key (for LLM intent parsing — free tier)
OPENROUTER_API_KEY=sk-or-...

# LangSmith Tracing (optional but recommended for demo)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=lsv2_...
LANGCHAIN_PROJECT=aegis-ml

# Modal Token (required for remote GPU execution)
MODAL_TOKEN_ID=...
MODAL_TOKEN_SECRET=...

# Optional: Custom model overrides
# INTENT_MODEL=arcee-ai/trinity-large-preview:free
# DEFAULT_GPU=a10g
